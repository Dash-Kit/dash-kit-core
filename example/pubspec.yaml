name: example
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  build_runner: ^2.0.6
  built_value: ^8.1.1
  built_value_generator: ^8.0.6
  cupertino_icons: ^1.0.2
  dash_kit_core: ^3.0.0-dev.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true